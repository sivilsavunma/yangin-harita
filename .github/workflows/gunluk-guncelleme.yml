name: Gunluk Harita Guncelleme

on:
  schedule:
    - cron: '0 6 * * *'  # Her sabah 09:00 (UTC+3)
  workflow_dispatch:     # Manuel çalıştırma desteği

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      OWM_API_KEY: ${{ secrets.OWM_API_KEY }}
      TOKEN: ${{ secrets.GH_PAT }}

    steps:
      - name: Repo'yu klonla
        uses: actions/checkout@v3

      - name: Python kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: 3.10

      - name: Gerekli paketleri yükle
        run: pip install requests

      - name: Harita dosyasını üret
        run: python harita_uret.py

      - name: Güncellenmiş dosyayı GitHub'a gönder
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🔄 Otomatik güncelleme: $(date -u '+%Y-%m-%d %H:%M:%S') UTC"
          git push https://x-access-token:${TOKEN}@github.com/sivilsavunma/yangin-harita.git
