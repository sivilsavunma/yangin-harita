name: Gunluk Harita Guncelleme

on:
  schedule:
    - cron: '0 6 * * *'  # Her sabah 09:00 Türkiye saati (UTC+3)
  workflow_dispatch:      # Manuel başlatma desteği

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      OWM_API_KEY: ${{ secrets.OWM_API_KEY }}   # Hava durumu API anahtarın
      TOKEN: ${{ secrets.GH_PAT }}              # GitHub Personal Access Token

    steps:
    - name: Repo'yu klonla
      uses: actions/checkout@v2

    - name: Python kurulumu
      uses: actions/setup-python@v2
      with:
